<%= form_for [@project, @entry] %>
	<%= f.label :hours %>
	<%= f.text_field :hours %>

	<%= f.label :minutes %>
	<%= f.text_field :minutes %>

	<%= f.label :date %>
	<%= f.date_field :date %>

	<%= f.label :comments %>
	<%= f.text_field :comments %>

	<%= f.submit 'Save' %> 
<%end%>